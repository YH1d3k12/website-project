import { PlayerData } from '@/service/player';

const playerData: PlayerData = {
    playerBalance: 100,
    casinoBalance: 10000,
    balanceHistory: [100],
    gameOver: false,
};

export default playerData;
